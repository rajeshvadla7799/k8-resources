apiVersion: v1
kind: Pod

metadata:
  name: resources-demo
  namespace: roboshop

  spec:
  containers:
    - name: nginx
      image: nginx:1.14.2
  resources:
  # soft limit
    requests:
      memory: "64Mi" #Ram
      cpu: "100m"

  # hard limit
    requests:
      memory: "128Mi" #Ram
      cpu: "150m"