apiVersion: v1
kind: Service

metadata:
  name: nginx

spec:
  selector:
    # labels used to identify matching pods
    project: roboshop
    component: frontend
    environment: dev

  ports:
    - protocol: TCP
      port: 80 # service port which is exposed by the service
      targetPort: 80 # container port which is exposed by the pod