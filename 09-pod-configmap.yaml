apiVersion: v1
kind: Pod
metadata:
  name: pod-configmap-demo
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    envFrom:
      - configMapRef:
          name: nginx-config